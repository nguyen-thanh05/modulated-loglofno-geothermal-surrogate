[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "geothermal-surrogate-research"
version = "0.1.0"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["training*", "models*", "losses*", "evaluation*"]
